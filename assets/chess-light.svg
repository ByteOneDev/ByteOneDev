<svg xmlns="http://www.w3.org/2000/svg" width="495" height="134" viewBox="0 0 495 134" role="img" font-family="'Segoe UI',Ubuntu,Helvetica,Arial,sans-serif"><defs></defs><style>
@keyframes fadeUp { from { opacity:0; transform:translateY(6px);} to {opacity:1; transform:translateY(0);} }
@keyframes grow { from { transform:scaleX(0);} to {transform:scaleX(1);} }
@keyframes float { 0%,100% { transform:translateY(0);} 50% { transform:translateY(-5px);} }
.fu { animation: fadeUp .7s ease both; }
.bar { transform-origin: left center; animation: grow 1.1s cubic-bezier(.22,1,.36,1) both; }
text { dominant-baseline: middle; }

    .h { font-size: 15px; font-weight: 600; }
    .l { font-size: 13px; font-weight: 500; }
    .v { font-size: 15px; font-weight: 700; }
    .s { font-size: 11px; }
    </style><rect x="0.5" y="0.5" width="494" height="133" rx="10" fill="#ffffff" stroke="#d0d7de"/><text class="h fu" x="25" y="32" fill="#0969da">♟ Échecs — Lichess</text><text class="s fu" x="470" y="32" fill="#59636e" text-anchor="end">@matt0967</text><text class="l fu" x="25" y="70" fill="#59636e">Compte non configuré — renseigne ton pseudo dans widgets.config.json</text></svg>